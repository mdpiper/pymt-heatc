# An env for re/building a C pymt-lab component with Meson.
name: cbld
channels:
  - conda-forge
dependencies:
  - python =3
  - pip
  - make
  - cmake
  - pkg-config
  - bmi-c
  - c-compiler
  - cython
  - meson-python
  - numpy
  - bmi-tester >=0.5.5
  - pymt >=1.3
